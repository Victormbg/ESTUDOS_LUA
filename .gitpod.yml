image:
  file: .gitpod.Dockerfile

tasks:
  - init: |
      clear
      lua -v
      luarocks --version
      git --version
      # Lista todos os pacotes instalados pelo luarocks
      luarocks list
